// This is the main schema file for the multi-file Prisma schema
// All datasource and generator configurations should be here

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
